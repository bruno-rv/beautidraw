# 08 — Vendor the initial asset set and build the manifest

Type: task
Status: open
Blocked by: 07
Parent: ../map.md

## Question

Populate `assets/` with a starter icon and image set in the format decided by 07, and produce the manifest in the decided schema.

Work to do:

- Pick and vendor the source set. Licence has to permit redistribution — the plugin is meant to be shareable, so anything not permissively licensed is disqualified regardless of how good it looks.
- Normalise the files: naming convention, sizing, stroke weight consistency, and whatever the format decision in 07 requires.
- Generate the manifest, by script if 07 chose a generated manifest.
- Record in the answer: how many assets, from where, under what licence, and the exact command that regenerates the manifest.

## Answer

<!-- on resolution: what landed, from where, under what licence -->
